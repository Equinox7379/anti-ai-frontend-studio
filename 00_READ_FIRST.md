# 先读这里：无上下文 AI 冷启动入口

这不是“禁止圆角、禁止卡片”的审美警察手册，也不是让模型随机抽一个品牌皮肤。它的目标是：当用户只提供灵感、业务目标、现有页面或项目源码时，让一个没有历史上下文的强 AI 亲自完成设计、实现、真实渲染、批评与修正，既避开生成式默认，也产出有任务价值、有作者判断、经得起使用的作品。

## 最简单的三阶段用法

平台对 ZIP 内多文件读取不稳定时：

```text
阶段一：ONE_FILE_MODE.md
完成设计、实现与第一版真实渲染

阶段二 A：POST_RENDER_REVIEW.md
先完成不受检测器锚定的设计评审并保存记录

阶段二 B：POST_RENDER_EVIDENCE_AND_REVISE.md
再运行检测、查反模式、综合并修改实际代码
```

其他目录是按需深读、案例、参考、模板和原始资料。负面清单不应在第一版构图前进入活动上下文。

平台能够浏览多个文件时，按以下顺序：

```text
core/01_DESIGN_DIRECTOR_CORE.md
core/04_ROUTING_INDEX.md
按任务选 0—2 个 knowledge/ 文件
完成第一版真实渲染
review_after_first_render/00_READ_AFTER_FIRST_RENDER.md
core/02_RENDER_REVIEW_REVISE.md
core/03_CRAFT_AND_DELIVERY.md
```

## 给执行 AI 的工作授权

你是本轮设计负责人和实现者。只要当前工具允许，就自行完成需求理解、现有项目阅读、信息架构、视觉设计、代码、运行、截图和修正。不要把“请选择字体／色板／三套布局”重新推给用户，也不要只交一份方案说明后停止。

仅当存在会实质改变产品方向的矛盾、缺少不可替代的业务事实、需要未获授权的不可逆操作或工具客观无法访问必要材料时，才提出一个最小澄清问题。普通设计选择、编译错误、响应式问题和可逆实现问题在同一任务中闭环。

## 现有项目与长期项目

- 改版先审计已有技术栈、视觉世界、真实状态和稳定心智模型；不从空白风格词重做。
- 多页面长期项目可创建 `PRODUCT_CONTEXT.md` 保存跨页面稳定事实；单页任务不必增加文件。
- 用户对已有页面说“加力／收声／提炼／加固／精修／澄清／适配／体检”时，读取 `DESIGN_VERBS.md` 做一次有边界的改进。

## 必须完成的六件事

1. 把用户灵感翻译为任务、心理感受、功能后果和技术约束，而不是直接翻译成熟悉皮肤。
2. 找到页面唯一命题、主要对象和主信息关系。
3. 根据不确定度做一至三套真正不同的结构探索；方向明确时不要强制三套。
4. 产出实际页面或可运行原型，并使用真实内容、长中文、数字、空值和错误状态。
5. 桌面和移动真实渲染后，先做 A 通道独立评审，再做 B 通道检测与综合；修改真实代码。
6. 交付代码、关键截图、简短 `PROJECT_DESIGN.md`、实际删除或修正的内容，以及仍未验证的客观限制。不得自评分。

## 立即失败的行为

- 只说设计理念，不产出页面；
- 未查看现有项目就重做视觉世界；
- 把用户形容词自动变成流行配方；
- 将同一业务关系拆成一排同型方块；
- 用获奖站、组件演示或搜索第一名定义产品工作流；
- 先读检测器和反模式，再围绕它们构图；
- 做完第一稿不渲染、不看图、不修正；
- 用扫描器零命中、测试通过或自评高分替代视觉判断；
- 让用户逐区块审批普通可逆设计选择。

## 资料分层

```text
core/                         主动设计、复核与交付核心
knowledge/                    按任务深入的专业方法
review_after_first_render/    第一版渲染后才加载的反退化材料
DESIGN_VERBS.md               已有页面的有边界反馈词汇
optional/                     轻量变体等按需机制
templates/                    项目设计、产品事实、施工与视觉契约
cases/                        正例、反例和修复链；学习关系，不抄皮肤
references/                   66 条深读参考；只迁移因果原则
blind_test/                   无上下文模型对照测试方法
archive/                      两个原始包、外部评审与完整研究遗产
```

本包是 `v6.0.0-rc2`。结构与工具可验证不等于冷启动效果已证明；真正有效性由后续盲测与实际项目结果决定。
