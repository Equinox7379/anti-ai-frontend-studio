# Anti-AI Frontend Studio v7.0.0-alpha6 · Runtime/Lab Distribution Revision 2

日常只使用 `anti-ai-frontend-studio-v7.0.0-alpha6-runtime-r2.zip` 与中文一键提示词。

`Lab r2` 是完整研究、测试、案例与发行构建源；不要在普通项目中上传给设计 AI。

Revision 2 新增默认封闭 Runtime 白名单、精确变更审计、活动上下文增长检查，以及与 Runtime 同次构建的 `meta/FILE_INDEX.*`、`meta/ALLOWLIST_DIFF.json` 和 `meta/VALIDATION_REPORT.json`。

当前仍为研究原型：`effectiveness_proven=false`，有效对照基准为 0 行。
