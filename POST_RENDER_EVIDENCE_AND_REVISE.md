# POST RENDER REVIEW · 阶段二 B：证据、综合与真实修正

只有在 A 通道观察已经保存后才读取本文件。现在可以运行检测器、查看反模式，并将两条通道综合成实际改动。

# B 通道：确定性检测与浏览器证据

本通道在 A 通道记录完成后执行。它只回答“源码或页面中实际出现了什么”，不回答“这个设计好不好”。

## 1. 静态检测

```bash
node tools/slop_detector.mjs <实际前端源码目录>
node tools/slop_detector.mjs <实际前端源码目录> --json
```

检测器是零依赖、描述性工具。它可能提示：`transition: all`、渐变文字、常见默认字体配对、占位文案、三等宽网格、大圆角、彩色发光、装饰编号，以及多项信号共同形成的高风险配方。

## 2. 浏览器事实

按任务需要检查：

- computed font、字号、字重、行高和数字特性；
- 主要有界表面的数量与职责；
- 横向溢出、触控区、焦点、loading／error／empty；
- 真实视口中的换行、密度和布局顺序；
- 相同状态的修改前后截图。

## 3. 结果格式

```text
DETECTOR_FINDINGS =
BROWSER_FACTS =
POSSIBLE_FALSE_POSITIVES =
UNAVAILABLE_EVIDENCE =
```

每项必须包含文件／选择器／视口等定位。单个颜色、字体、圆角或网格不能自动定罪；组合信号仍需回到业务语义判断。

## 4. 禁止

- 不输出总分；
- 不把零命中称为“无 AI 味”；
- 不因低置信提示自动改设计；
- 不让检测结果覆盖 A 通道的初始观察；
- 不用私有类名替代用户可见事实。

---

# 失效机制：来自六 AI 团队 v2.3 与本包综合

## M-A 合规最大化病

模型把允许上限当目标，把卡片、颜色、Signature、非对称和动效预算全部用满。修正：上限只触发怀疑；真实删除一项；不强制每页拥有 Signature 或“非模板结构”。

## M-B 映射只搬皮肤

模型声称参考成熟产品，实际只换字体类别、色板和圆角。修正：参考必须写因果；字体迁移必须落到字形、字号、字重、字宽、字距、行高、数字、换行与结构装置。

## M-C 自评降级表演

模型用成熟语言软化用户否定，保留失败设计。修正：准确记录用户原话；保留 rejected 样本；区分意图、实现、渲染与用户判断；不以自评分或测试背书。

## M-D 生成—审查混线（综合所得）

大量反模式在生成前进入上下文，使设计围绕禁令收敛到无性格方案。修正：先使用主动设计核心形成第一版，再加载本目录。

---

# GPT 默认组合风险

这些材料都可能合法；危险在于它们不随 Brief 改变，成套自动出现。

## 配方 A：假高级编辑式

```text
#F4F1EA 或相近奶油底
+ 大型衬线／宋体标题
+ 砖红／陶土点缀
+ 发丝线、眉题、小编号
```

## 配方 B：反 slop 黑酸风

```text
纯黑／近黑
+ 荧光绿或朱红
+ 巨型 grotesk／等宽
+ 跑马灯、噪点、自定义光标
```

## 配方 C：报纸式无卡片模板

```text
零圆角
+ 密集分栏
+ 大量 1px 线
+ 01 / 02 / 03、角标、眉题
```

## 其他常见组合

- Inter + Playfair Display／Instrument Serif；
- 紫蓝渐变＋深色 mesh＋三等宽功能卡；
- 左侧栏＋四 KPI＋图表＋最近活动；
- Apple 式巨型居中标题＋玻璃导航＋Bento；
- Linear 式近黑＋Geist／Inter＋冷灰边框＋命令面板。

判断：逐项说明其项目来源。无法说明时，不要“弱化一点”，而是回到任务关系重做。

---

# AI 味高信号清单（只用于首稿后诊断）

## 结构

- 任意内容都套卡；卡片内再套卡；
- 同尺寸“图标＋标题＋说明”成为页面骨架；
- 连续流程拆成组件节点；
- Hero、三列功能、四 KPI、右侧伪截图不随业务变化；
- 直角栏目墙假装去卡片化；
- 移动端把所有桌面模块纵向堆叠。

## 排版

- Inter／Geist 通吃，或与流行展示衬线自动配对；
- 大标题、眉题、01 编号、细线作为填空；
- 小灰字过多，页面看似克制实则难读；
- 中文照搬英文负字距和海报行高；
- 等宽字体只为“技术感”。

## 颜色与表面

- 紫蓝渐变、光晕、玻璃、无理由阴影；
- 奶油＋绿／砖红；黑＋荧光；
- 每个状态都是胶囊；每个标题都有图标底座；
- 左侧彩色竖线泛滥；
- 多个颜色同时表示当前、成功、交互和装饰。

## 动效与文案

- 全页 fade-up、统一上浮／缩放；
- 动效只为让交付看起来做了事；
- “Phase 0、样板槽位、结构样例、组件示范”泄漏；
- 虚构保存时间、版本、在线状态或“AI 正在分析”；
- 高级、赋能、智能、无缝等空洞话术。

## 反向误区

- 为了避开上述信号，把页面做成没有层级的文本表；
- 故意随机错位、加手绘线和噪点制造“人味”；
- 只换字体或颜色，骨架不变；
- 用扫描器零命中证明作品优秀。

---

# 综合两条通道并修改真实作品

现在 A、B 均已完成。不要简单拼接报告。

## 1. 综合矩阵

```text
A 与 B 一致：
A 发现、B 无法检测：
B 发现、A 漏看：
B 的假阳性或合理例外：
仍无法验证：
```

A 通道对设计特异性、层级、情绪和任务负责；B 通道对源码信号、几何、状态和实现事实负责。任何一方都不能取代另一方。

## 2. 修正优先级

```text
任务受阻、数据或状态误导
→ 信息架构与主关系
→ 层级、密度和响应式
→ 字体、表面、颜色与动效
→ 光学和代码精修
```

只处理真实问题。检测器提示但经页面语义证明合理的内容，不必为了清零而删除。

## 3. 实际修改

至少对最重要问题修改代码并重新渲染。记录：

```text
PRIORITY_PROBLEM =
ROOT_CAUSE =
CODE_CHANGE =
RENDERED_RESULT =
PRESERVED_STRENGTH =
```

若第一稿确实成立，不强造修改；但必须给出真实路径和渲染证据，而不是一句“全部通过”。

## 4. 结论

不得产生生成者自签的设计分。最终说明应区分：已证实、合理推测、检测线索、假阳性和未验证项。

---

# 完成要求

1. 说明 A 与 B 的一致、冲突、遗漏和假阳性。
2. 只修改对真实用户、任务、层级、状态或项目特异性有影响的问题。
3. 重新渲染相同状态，确认问题确实改善且原有强项未被破坏。
4. 不追求检测器零命中，不输出设计健康总分，不以扫描结果取代视觉判断。
5. 最终记录实际代码改动、已验证视口与状态，以及仍未知的事项。
