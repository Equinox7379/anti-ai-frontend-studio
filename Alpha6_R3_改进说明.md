# Alpha6 R3：执行链对齐与任务优先设计

日期：2026-09-05。基于交接包的 Alpha6 Runtime R2 / Lab R2 制作。这是分发修订 R3，不是新一套 Alpha7 理论；没有修改或推送远端仓库。Lab 保留研究馆藏，Runtime 仍从默认封闭白名单构建。

## 设计侧：少重复说明，早一点面对真实页面

运行入口改为按阶段读取。包内有案例和诊断，不代表 Author 在落笔前必须读完整套材料。先认清实际对象和用户的主要动作，确定项目张力、身份锚点、生成法则及取舍，再尽快打通输入或选择、操作、可观察结果和必要恢复的一条真实任务。文字说明服务于实现，不再要求把紧凑蓝图重复扩写为数份同义文档。

Author 指导补上中文长标题、零值与缺失值的区别、数字尺度、字体角色和移动端任务次序。它们必须由项目内容决定，不是预制配色与页面骨架。M 默认一套有依据的方案；只有真实结构分歧才比较候选。Guardian 必须指出值得保留的设计，不为制造修订而制造变化，不机械要求每次运行检测器、像素差异或 pairwise。

当前白名单仍为 24 个活动文档路径，按同一口径计量为 54,868 字节，R2 为 53,548 字节。该数值是文档字节数，不是模型 token、实际必读量或设计质量指标。新增的组件夹具和测试只进入 Lab。

## 修复的可复现问题

| R2 的问题 | R3 的实际修改 |
|---|---|
| 默认精简蓝图使用 task_truth / active_design_rules，但追踪、真实性检查及终稿工具读取旧字段，误报规则不存在并漏查虚假保存文案 | 用只读 blueprint_model 统一精简与完整蓝图入口；消费工具支持当前字段，保留旧格式兼容；同目录两份活动蓝图拒绝静默择一 |
| 没有提供任何项目证据，assurance_gate 仍能输出 READY；嵌套 Guardian 失败未被正确判定 | 缺证据返回 NEEDS_WORK，真实失败返回 BLOCKED；检查目标渲染、相关回归、最终功能结论和实际截图引用；不把浏览器 canary 当项目验收 |
| 独立 Guardian ZIP 缺少 artifact_doctor 的导入依赖；Author ZIP 缺入口要求的 Trace 模板 | 补足真实依赖和模板，构建时检查包内本地模块导入闭合；用独立解压目录启动工具验证 |
| 当前独立性模板与部分消费者格式不一致，手填平台标识可能造成保障级别误读 | 当前模板可正确顺序自审；离线收口最高 L1，真实宿主独立上下文需另有平台事实，不能自行写几个 ID 升级 |
| 影响范围只看到部分 Git 改动；S 路径依赖 Lab-only 的回归模块并可能静默跳过 | 合并已暂存、未暂存、未跟踪与预告路径；S 直接检查真实测试入口和回归结果，不依赖 Lab 模块 |
| expect_text 立即读文本，异步组件易误失败；空步骤和空视口可漏检 | 文本、值、属性、数量与焦点采用有上限等待的断言；空场景、真实失败步骤、HTTP 错误页和全页溢出不能静默通过 |
| Runtime 构建输出目录可被不加区分地替换 | 拒绝覆盖不属于生成 Runtime 的非空目录，不伤害无关文件 |
| 两套包与内部隔离 ZIP、索引容易分别更新 | 新增 Lab-only build_distribution，一次生成两套包并同步 Packet 和同次构建索引；不擅自执行大套测试或安装浏览器 |

这些检查不是密码学溯源、万能真实性检测或审美判分。来源定位、词法提示、截图解码与报告一致性只能证明各自负责的有限事实，无法阻止所有伪造。

## 已经实际执行的验证

现有合同、档位、真实性、独立性、参考预算、回归、Alpha6 加固、防回流、发行拆分和检测器测试，加上新增 30 项执行合同测试，共 11 个测试脚本已在本轮云端运行通过。详细逐脚本结果见 `distribution/R3_TEST_RESULTS.json`。旧完整蓝图样例仍通过相关测试。

新增的 `examples/component-workbench` 是拆分模型、列表和原位编辑组件的原生 ES Modules 夹具。实际运行桌面与手机尺寸、首稿与修复稿共 92 个浏览器步骤；覆盖异步更新、空标题错误、长中文标题、零时间、取消和焦点恢复。首稿只在临时副本故意植入虚假保存状态，检出后修复真实源码；六项模型测试按同一计划前后通过，Trace、真实性检查、两阶段 Artifact Doctor、Workflow Doctor 和 M 档收口跑通。另有真实失败断言证明错误不会被当作 PASS。

当前云端 Chromium 的管理策略阻止了本机 HTTP 导航。没有修改策略。组件验证显式使用离线方式嵌入同一组本地 ES Module 的实际代码，渲染和交互真实执行，HTTP 导航明确为 NOT_TESTED。桌面和移动截图已由云端查看，但该夹具不是美学对照实验，不计入有效比较样本。

## 使用及维护

日常只上传 `anti-ai-frontend-studio-v7.0.0-alpha6-runtime-r3.zip`，配合其中的 `PROMPT_AUTORUN.txt` 与真实项目材料。Lab 用于维护、研究和构建，不进入普通 Author 活动上下文。仍由当前任务和 AGENTS.md 决定 Git 权限，没有默认远端发布授权。

从 Lab 重建 Runtime 使用 `python tools/build_runtime.py --out ../anti-ai-frontend-studio-v7.0.0-alpha6-runtime-r3 --zip ../anti-ai-frontend-studio-v7.0.0-alpha6-runtime-r3.zip`。同时生成两套分发使用 `python tools/build_distribution.py --out ../studio-distributions`。新内容默认不进入 Runtime；新增模块和 Packet 成员已按现有政策记录，不增加另一套治理系统。

## 没有被证明的部分

未完成 Windows 运行、React/Vue/Svelte 专项集成、真实用户项目盲评或跨模型同预算对照。没有声称绝对去除 AI 味，也没有把工具 PASS 当成审美通过。`effectiveness_proven=false`，有效比较样本仍为 0，自动交付上限仍为 READY_FOR_OWNER_VISUAL_REVIEW。
